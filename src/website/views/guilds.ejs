<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/public/css/guilds.css">
    <title>Guilds</title>
</head>

<body>
    <p>seus servidores</p>
    <div id="guilds"> <% if(guilds)  
        for(const guild of guilds) { %>
        <div id="guild">
            <% if(guild.icon) { %>
            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.jpg?size=64">
            <% } else { %>
            <img src="https://cdn.discordapp.com/icons/515688359373438978/3f4da4742bcc0e64e74863c5f856d8dc.jpg?size=64">
            <% } %>
            <p>
            <% if(guild.name.length > 15) { %>
                <%= guild.name.slice(0, 15) + '...' %>
            <%} else{%>
                <%= guild.name %>
            <% } %></p>
            <button><a href="dashboard/<%= guild.id %>/index">Manage</a></button>
            <button><a href="dashboard/leave/<%= guild.id %>">Leave</a></button>
        </div>
        <% } else { %>
        <p>Parece que você não tem servidores...</p>
        <% } %>
    </div>
</body>

</html>